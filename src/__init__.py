"""
src — Core library modules for the vision-based desktop automation project.

Modules:
    grounding   — Cascaded Gemini visual grounding engine
    automation  — Notepad control (launch, type, save, close)
    api_client  — JSONPlaceholder API client with fallback
    fallback    — BotCity template-matching fallback
"""
